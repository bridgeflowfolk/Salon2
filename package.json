{
  "name": "bff-salon",
  "version": "1.0.0",
  "description": "Site Salon Bien-Être et Créateurs – Bridge & Flow Folk",
  "scripts": {
    "dev":   "npx @11ty/eleventy --serve --watch",
    "build": "npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
